name: CI/CD

on:
  push:
    branches: [ master ]

jobs:
  build:
    runs-on: self-hosted

    steps:
    - name: Deploy with React Build
      run: |
        sudo git pull origin master
        sudo sh /var/www/roadmap/deploy.sh
