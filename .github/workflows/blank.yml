name: CI

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    runs-on: self-hosted

    steps:
    - name: Deploy with React Build
      run: |
        sudo sh /var/www/roadmap/deploy.sh